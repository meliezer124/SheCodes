# Read the arrays (assuming that they're always length of 3)
# Get the two middle elements
# Join the two middle elements in a new array
# Return a new array with 2 elements

def middle_way(a, b):
  middle_a = a[1]
  middle_b = b[1]
  new_array = [middle_a, middle_b]
  return new_array